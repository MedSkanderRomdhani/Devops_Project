pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build') {
            steps {
                script {
                    // Set Maven home, assuming it's configured in Jenkins
                    def mvnHome = tool 'Maven'
                    def mavenCmd = "${mvnHome}/bin/mvn"

                    // Maven build command
                    sh "${mavenCmd} clean install"
                }
            }
        }

        stage('Run Tests') {
            steps {
                script {
                    // Assuming your project has a test phase in the Maven lifecycle
                    def mvnHome = tool 'Maven'
                    def mavenCmd = "${mvnHome}/bin/mvn"

                    // Maven test command
                    sh "${mavenCmd} test"
                }
            }
        }

        stage('Deploy') {
            steps {
                // Add deployment steps here if needed
            }
        }
    }

    post {
        success {
            echo 'Build and tests passed. Ready for deployment.'
        }
        failure {
            echo 'Build or tests failed. Check the Jenkins console output for details.'
        }
    }
}
